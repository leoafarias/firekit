import "reflect-metadata";

// Global test setup
beforeAll(() => {
  // Common setup for all tests
  console.log("Starting tests for Repokit");
});

afterAll(() => {
  // Common teardown for all tests
  console.log("Finished tests for Repokit");
});
